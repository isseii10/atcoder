#include <bits/stdc++.h>
#define rep(i, n) for (int i=0; i < (n); i++)
using namespace std;
typedef long long ll;
typedef pair<int, int> P;

int main() {
    int a, p;
    cin >> a>>p;
    int kakera = 0;
    kakera =  p + a*3;
    cout << kakera / 2 << endl;
}